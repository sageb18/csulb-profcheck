{
  "name": "csulb-profcheck",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/background.js src/content.js --bundle --outdir=dist --alias:fs=./fs-stub.js",
    "watch": "esbuild src/background.js src/content.js --bundle --outdir=dist --alias:fs=./fs-stub.js --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "esbuild": "^0.28.2"
  },
  "dependencies": {
    "rate-my-professor-api-ts": "^1.0.5"
  }

}
