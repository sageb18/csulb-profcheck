{
  "name": "csulb-profcheck",
  "version": "1.0.0",
  "description": "Chrome extension that shows Rate My Professors ratings next to instructor names on the CSULB class search page.",
  "private": true,
  "scripts": {
    "build": "esbuild src/background.js src/content.js src/styles.css --bundle --outdir=dist",
    "watch": "esbuild src/background.js src/content.js src/styles.css --bundle --outdir=dist --watch",
    "package": "npm run build && powershell -Command \"Compress-Archive -Path manifest.json,dist,icons -DestinationPath profcheck.zip -Force\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
